# Федеральное государственное автономное образовательное учреждение высшего образования
# Национальный исследовательский университет ИТМО
# Факультет программной инженерии и компьютерной техники
# Направление подготовки 09.03.04 Программная инженерия
# Дисциплина «Распределенные системы хранения данных»

# Отчет по лабораторной работе №2

Вариант: `[указать вариант]`

Студент: `[ФИО]`  
Группа: `[группа]`

Преподаватель: `[ФИО преподавателя]`

г. Санкт-Петербург, 2026 г.

---

## Оглавление

1. [Задание](#задание)
2. [Выполнение](#выполнение)
3. [Этап 1. Резервное копирование](#этап-1-резервное-копирование)
4. [Расчет объема резервных копий](#расчет-объема-резервных-копий)
5. [Этап 2. Потеря основного узла](#этап-2-потеря-основного-узла)
6. [Этап 3. Повреждение файлов БД](#этап-3-повреждение-файлов-бд)
7. [Этап 4. Логическое повреждение данных](#этап-4-логическое-повреждение-данных)
8. [Ответы на вопросы для защиты](#ответы-на-вопросы-для-защиты)
9. [Вывод](#вывод)

---

## Задание

Цель работы - настроить процедуру периодического резервного копирования базы данных, сконфигурированной в ходе выполнения лабораторной работы №1, а также разработать и отладить сценарии восстановления в случае сбоев.

Узел из предыдущей лабораторной работы используется в качестве основного. Новый узел используется в качестве резервного. В сценариях восстановления используется копия данных, полученная на первом этапе данной лабораторной работы.

Требуется выполнить:

### Этап 1. Резервное копирование

- настроить резервное копирование с основного узла на резервный по схеме: первоначальная полная копия + непрерывное архивирование WAL;
- включить режим архивирования WAL;
- настроить копирование WAL на резервный узел с помощью `scp`;
- создать первоначальную резервную копию с помощью `pg_basebackup`;
- перенести базовую копию на резервный узел с помощью `rsync`;
- оценить объем резервных копий спустя месяц при условиях:
  - средний объем новых данных в БД за сутки: `200 MB`;
  - средний объем измененных данных за сутки: `650 MB`.

### Этап 2. Потеря основного узла

Необходимо восстановить работу СУБД на резервном узле, продемонстрировать успешный запуск сервера и доступность данных.

### Этап 3. Повреждение файлов БД

Необходимо:

- симулировать сбой удалением директории любого табличного пространства со всем содержимым;
- проверить работу СУБД и доступность данных;
- перезапустить СУБД и проанализировать результат;
- выполнить полное восстановление данных на основном узле из резервной копии;
- разместить восстановленные данные в другой директории, так как исходный `PGDATA` считается недоступным.

### Этап 4. Логическое повреждение данных

Необходимо:

- добавить в каждую таблицу 2-3 новые строки;
- зафиксировать время;
- удалить каждую вторую строку в одной из таблиц;
- выполнить восстановление данных на основном узле способом `pg_dump` на резервном узле с последующим применением файла на основном узле;
- продемонстрировать результат и выполнить анализ.

---

## Выполнение

В работе использовались два узла:

- основной узел: `postgres0@pg125`;
- резервный узел: `postgres2@pg132`.

Подключение к обоим узлам выполнялось через промежуточный узел `helios`:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres2@pg132
```

Параметры кластера из лабораторной работы №1:

- каталог данных: `$HOME/nwc36`;
- порт: `9099`;
- база данных: `bigbluecity`;
- табличные пространства:
  - `$HOME/sbm10`;
  - `$HOME/nym69`.

Для автоматизации были подготовлены файлы:

- `lab2/scripts/env.sh`;
- `lab2/scripts/stage1_backup.sh`;
- `lab2/scripts/calc_backup_size.sh`;
- `lab2/scripts/stage2_failover.sh`;
- `lab2/scripts/stage3_restore_primary.sh`;
- `lab2/scripts/stage4_prepare.sql`;
- `lab2/scripts/stage4_restore_from_reserve.sh`.

---

## Этап 1. Резервное копирование

Сначала был подготовлен файл переменных окружения `lab2/scripts/env.sh`, в котором были указаны узлы, пути и каталоги резервного копирования.

Фрагмент конфигурации:

```bash
JUMP_SSH="s413099@helios.cs.ifmo.ru"
JUMP_PORT="2222"

PRIMARY_HOST="pg125"
PRIMARY_USER="postgres0"
STANDBY_HOST="pg132"
STANDBY_USER="postgres2"

PRIMARY_SSH="${PRIMARY_USER}@${PRIMARY_HOST}"
STANDBY_SSH="${STANDBY_USER}@${STANDBY_HOST}"

PRIMARY_PGDATA='${HOME}/nwc36'
PRIMARY_PORT="9099"
PRIMARY_DB="bigbluecity"
```

Далее на основном узле была настроена архивизация WAL и создана первоначальная полная копия.

Команда запуска сценария:

```bash
cd /Users/skadibtw/ddss/lab2
bash scripts/stage1_backup.sh
```

Основные действия сценария:

1. Создание каталогов резервного копирования и архива WAL на основном и резервном узлах.
2. Настройка параметров сервера PostgreSQL на основном узле:

```sql
ALTER SYSTEM SET wal_level = 'replica';
ALTER SYSTEM SET archive_mode = 'on';
ALTER SYSTEM SET archive_timeout = '300';
ALTER SYSTEM SET archive_command = 'test ! -f /tmp/ddss_lab2_archive/%f && cp %p /tmp/ddss_lab2_archive/%f && scp -q /tmp/ddss_lab2_archive/%f postgres2@pg132:/tmp/ddss_lab2_archive/%f';
```

3. Перезапуск кластера на основном узле для применения `archive_mode`.
4. Создание базовой физической копии:

```bash
pg_basebackup \
  -D '/tmp/ddss_lab2_backup/base' \
  -Fp \
  -X stream \
  -P \
  -c fast \
  --tablespace-mapping='${HOME}/sbm10=/tmp/ddss_lab2_backup/tblspc/sbm10' \
  --tablespace-mapping='${HOME}/nym69=/tmp/ddss_lab2_backup/tblspc/nym69'
```

5. Принудительное переключение WAL:

```sql
SELECT pg_switch_wal();
```

6. Перенос полной копии на резервный узел через `rsync`.

Пример содержимого сценария `stage1_backup.sh`:

```bash
ssh "${SSH_OPTS[@]}" "${PRIMARY_SSH}" "psql -v ON_ERROR_STOP=1 -p '${PRIMARY_PORT}' -d postgres <<'SQL'
ALTER SYSTEM SET wal_level = 'replica';
ALTER SYSTEM SET archive_mode = 'on';
ALTER SYSTEM SET archive_timeout = '300';
ALTER SYSTEM SET archive_command = '${ARCHIVE_COMMAND}';
SQL
"

ssh "${SSH_OPTS[@]}" "${PRIMARY_SSH}" "pg_ctl -D ${PRIMARY_PGDATA} restart -m fast"

ssh "${SSH_OPTS[@]}" "${PRIMARY_SSH}" "pg_basebackup -D '${BACKUP_ROOT}/base' -Fp -X stream -P -c fast --tablespace-mapping='${PRIMARY_TS1}=${BACKUP_ROOT}/tblspc/sbm10' --tablespace-mapping='${PRIMARY_TS2}=${BACKUP_ROOT}/tblspc/nym69'"
```

Проверка параметров архивирования:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "psql -v ON_ERROR_STOP=1 -p 9099 -d postgres -c 'SHOW archive_mode; SHOW archive_command; SHOW wal_level;'"
```

Пример ожидаемого вывода:

```text
 archive_mode
-------------
 on

 archive_command
--------------------------------------------------------------------------------
 test ! -f /tmp/ddss_lab2_archive/%f && cp %p /tmp/ddss_lab2_archive/%f && ...

 wal_level
-----------
 replica
```

Проверка наличия резервной копии и WAL-архива на резервном узле:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres2@pg132 \
  "ls -lah /tmp/ddss_lab2_backup /tmp/ddss_lab2_archive"
```

Пример ожидаемого результата:

```text
/tmp/ddss_lab2_archive:
000000010000000000000001
000000010000000000000002

/tmp/ddss_lab2_backup:
base/
tblspc/
```

Для работы `scp` внутри `archive_command` на основном узле был настроен вход по SSH-ключу на резервный узел без ввода пароля.

---

## Расчет объема резервных копий

В данной работе используется схема:

- одна полная физическая копия;
- последующее непрерывное архивирование WAL.

Предположим, что минимальный объем архивируемых WAL за сутки примерно равен сумме объема новых и измененных данных:

```text
Vwal_day = 200 MB + 650 MB = 850 MB
```

За 30 суток:

```text
Vwal_month = 850 * 30 = 25500 MB
```

Переведем в гигабайты:

```text
25500 / 1024 ≈ 24.90 GB
```

Итоговый объем хранения через месяц:

```text
Vtotal = Vbase + 25500 MB
```

где:

- `Vbase` - размер полной физической копии;
- `25500 MB` - оценка объема архивных WAL за 30 суток.

Если, например, исходная полная копия имеет размер `1.2 GB`, то:

```text
Vtotal ≈ 1.2 GB + 24.9 GB = 26.1 GB
```

### Анализ

Можно сделать следующие выводы:

- основной вклад в объем резервных копий дает не базовая копия, а архив WAL;
- чем выше интенсивность изменений, тем быстрее растет архив;
- рассчитанное значение является нижней оценкой;
- в реальной системе объем WAL будет больше из-за full page image, изменений индексов, `VACUUM`, фоновых процессов и служебных операций.

Следовательно, при практическом планировании объема диска необходимо закладывать дополнительный запас.

---

## Этап 2. Потеря основного узла

В этом сценарии предполагается полная недоступность основного узла. Для восстановления используется резервный узел и копия данных, полученная на этапе 1.

Команда запуска сценария:

```bash
cd /Users/skadibtw/ddss/lab2
bash scripts/stage2_failover.sh
```

Сценарий выполняет следующие действия на резервном узле:

1. Создает новый каталог данных `FAILOVER_PGDATA=/tmp/ddss_lab2_failover_pgdata`.
2. Копирует в него базовую копию из каталога `/tmp/ddss_lab2_backup/base`.
3. Добавляет параметры восстановления:

```text
restore_command = 'cp /tmp/ddss_lab2_archive/%f %p'
recovery_target_timeline = 'latest'
recovery_target_action = 'promote'
```

4. Создает файл `recovery.signal`.
5. Запускает PostgreSQL на резервном узле на порту `9099`.
6. Проверяет доступность БД `bigbluecity` и таблицы `sales`.

Ключевой фрагмент сценария:

```bash
rsync -aH --delete '${BACKUP_ROOT}/base/' '${FAILOVER_PGDATA}/'

cat >> '${FAILOVER_PGDATA}/postgresql.auto.conf' <<CONF
port = '${STANDBY_PORT}'
listen_addresses = 'localhost'
unix_socket_directories = '/tmp'
restore_command = 'cp ${ARCHIVE_DIR}/%f %p'
recovery_target_timeline = 'latest'
recovery_target_action = 'promote'
CONF

touch '${FAILOVER_PGDATA}/recovery.signal'
pg_ctl -D '${FAILOVER_PGDATA}' -l '${FAILOVER_PGDATA}/startup.log' start
```

Проверка результата:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres2@pg132 \
  "psql -v ON_ERROR_STOP=1 -h localhost -p 9099 -d bigbluecity -c 'SELECT pg_is_in_recovery(), count(*) FROM sales;'"
```

Пример ожидаемого вывода:

```text
 pg_is_in_recovery | count
-------------------+-------
 f                 | 3000
```

Результат показывает, что кластер успешно поднят на резервном узле, восстановление завершено, а данные доступны для чтения.

---

## Этап 3. Повреждение файлов БД

Согласно условию задания, необходимо смоделировать потерю файлов одного из табличных пространств. Для этого была удалена директория `$HOME/sbm10` на основном узле.

Команда симуляции сбоя:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "rm -rf \$HOME/sbm10"
```

Проверка доступности данных до перезапуска:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "psql -v ON_ERROR_STOP=0 -p 9099 -d bigbluecity -c 'SELECT count(*) FROM products;'"
```

На этом этапе возможны два варианта:

- запрос завершается ошибкой при обращении к поврежденным файлам;
- часть запросов еще работает, если необходимые блоки находятся в памяти.

После этого выполняется перезапуск кластера:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "pg_ctl -D \$HOME/nwc36 restart -m fast"
```

После перезапуска сервер уже не может корректно использовать потерянное табличное пространство, что подтверждает физическое повреждение данных.

### Восстановление данных

По условию задания исходный каталог `PGDATA` считается недоступным, поэтому восстановление выполняется в новую директорию.

Команда запуска сценария:

```bash
cd /Users/skadibtw/ddss/lab2
bash scripts/stage3_restore_primary.sh
```

Сценарий выполняет:

1. Остановку поврежденного кластера на основном узле.
2. Создание нового каталога данных:

```text
$HOME/nwc36_restore
```

3. Восстановление базовой копии в новый `PGDATA`.
4. Восстановление каталогов табличных пространств в новые директории:

```text
/tmp/ddss_lab2_restore_ts1
/tmp/ddss_lab2_restore_ts2
```

5. Пересоздание ссылок в `pg_tblspc`.
6. Настройку `restore_command` для применения архивных WAL.
7. Запуск восстановленного кластера.

Фрагмент сценария:

```bash
rsync -aH --delete '${BACKUP_ROOT}/base/' '${PRIMARY_RESTORE_PGDATA}/'
rsync -aH --delete '${BACKUP_ROOT}/tblspc/sbm10/' '${PRIMARY_RESTORE_TS1}/'
rsync -aH --delete '${BACKUP_ROOT}/tblspc/nym69/' '${PRIMARY_RESTORE_TS2}/'

touch '${PRIMARY_RESTORE_PGDATA}/recovery.signal'

restore_command = 'cp /tmp/ddss_lab2_archive/%f %p'
```

Проверка доступности данных после восстановления:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "psql -v ON_ERROR_STOP=1 -h localhost -p 9099 -d bigbluecity -c 'SELECT pg_is_in_recovery(), count(*) FROM sales;'"
```

Дополнительно можно проверить новые пути табличных пространств:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "psql -p 9099 -d postgres -c 'SELECT spcname, pg_tablespace_location(oid) FROM pg_tablespace ORDER BY spcname;'"
```

Пример ожидаемого результата:

```text
   spcname   |     pg_tablespace_location
-------------+----------------------------------
 nym69_space | /tmp/ddss_lab2_restore_ts2
 sbm10_space | /tmp/ddss_lab2_restore_ts1
```

Таким образом, база данных была полностью восстановлена на основном узле в новом каталоге, а дополнительные табличные пространства были успешно перенесены в новые директории.

---

## Этап 4. Логическое повреждение данных

Для демонстрации логического восстановления была выбрана таблица `products`, поскольку она наглядна и не содержит внешних ключей, которые усложняли бы частичное восстановление.

На первом шаге в каждую таблицу были добавлены новые строки.

Команда запуска подготовительного SQL-сценария:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "cd /Users/skadibtw/ddss/lab2 && psql -v ON_ERROR_STOP=1 -p 9099 -d bigbluecity -f scripts/stage4_prepare.sql"
```

Сценарий выполняет:

- вывод количества строк до изменений;
- добавление по 2 строки в таблицы `customers`, `products`, `stores`, `sales`;
- вывод содержимого таблицы `products` после вставки;
- фиксацию времени восстановления `recovery_target_time`;
- удаление каждой второй строки из таблицы `products`;
- вывод итогового состояния таблицы `products` после ошибки.

Фрагмент `stage4_prepare.sql`:

```sql
INSERT INTO products (name, category, price) VALUES
('Lab2 SSD', 'Электроника', 9900.00),
('Lab2 Mouse', 'Аксессуары', 1900.00);

SELECT clock_timestamp() AS recovery_target_time \gset
\echo :recovery_target_time

DELETE FROM products
WHERE id IN (
  SELECT id
  FROM (
    SELECT id, row_number() OVER (ORDER BY id) AS rn
    FROM products
  ) s
  WHERE rn % 2 = 0
);
```

Пример ожидаемого фрагмента вывода:

```text
=== RECOVERY TARGET TIME ===
2026-03-21 18:42:11.512345+03

=== AFTER DELETE ===
 id |      name       |   category    |  price
----+-----------------+---------------+---------
  1 | Ноутбук         | Электроника   | ...
  3 | Наушники        | Аксессуары    | ...
  5 | Lab2 SSD        | Электроника   | 9900.00
```

Видно, что часть строк была удалена ошибочной операцией `DELETE`.

### Восстановление данных

Для восстановления используется резервный узел. На нем разворачивается временный кластер из базовой копии, затем выполняется PITR до времени непосредственно перед ошибочным удалением. После этого из полученного состояния выгружается таблица `products` с помощью `pg_dump`, а затем она восстанавливается на основном узле с помощью `pg_restore`.

Команда запуска:

```bash
cd /Users/skadibtw/ddss/lab2
TARGET_TIME='2026-03-21 18:42:11.512345+03' bash scripts/stage4_restore_from_reserve.sh
```

Сценарий делает следующее:

1. На резервном узле разворачивает временный кластер `RESERVE_STAGE4_PGDATA=/tmp/ddss_lab2_stage4_pgdata`.
2. Указывает параметры восстановления:

```text
restore_command = 'cp /tmp/ddss_lab2_archive/%f %p'
recovery_target_time = '2026-03-21 18:42:11.512345+03'
recovery_target_inclusive = 'true'
recovery_target_action = 'promote'
```

3. Запускает PITR на резервном узле.
4. Выполняет:

```bash
pg_dump -h localhost -p 9191 -d bigbluecity -Fc -t public.products -f /tmp/ddss_lab2_transfer/products_before_delete.dump
```

5. Передает полученный дамп на основной узел.
6. Выполняет на основном узле:

```bash
pg_restore --clean --if-exists --no-owner --no-privileges \
  -h localhost -p 9099 -d bigbluecity -t public.products \
  /tmp/ddss_lab2_transfer/products_before_delete.dump
```

Проверка результата:

```bash
ssh -J s413099@helios.cs.ifmo.ru:2222 postgres0@pg125 \
  "psql -v ON_ERROR_STOP=1 -h localhost -p 9099 -d bigbluecity -c 'TABLE products;'"
```

Ожидаемый результат: таблица `products` возвращается в состояние до ошибочного удаления, включая только что добавленные строки.

### Анализ

Этот способ восстановления удобен в ситуациях, когда:

- повреждена не вся БД, а отдельный объект;
- нужно минимизировать простой рабочей системы;
- необходимо восстановить данные на конкретный момент времени.

Комбинация физической базовой копии и архивных WAL позволила получить на резервном узле точное состояние БД перед ошибкой, а логический `pg_dump` дал возможность перенести только нужную таблицу обратно на основной узел.

---

## Ответы на вопросы для защиты

### 1. Резервные копии: назначение; локальные и удаленные; полные и инкрементные; холодные и горячие

Резервные копии нужны для восстановления после отказов оборудования, ошибок администратора, логического удаления данных и полной потери узла.

- локальные копии хранятся на том же сервере и быстро восстанавливаются, но не защищают от потери самого узла;
- удаленные копии хранятся на другом сервере и защищают от полной потери основного узла;
- полные копии содержат все данные целиком;
- инкрементные копии содержат только изменения относительно предыдущего состояния;
- холодные копии создаются при остановленной СУБД;
- горячие копии создаются без остановки сервера.

### 2. Сравнение подходов SQL Dump, FS Level Backup, Continuous Archiving

`SQL Dump`:

- логический уровень;
- удобен для отдельных БД и таблиц;
- хорошо переносится между версиями PostgreSQL;
- медленнее на больших объемах;
- восстановление тоже занимает много времени.

`FS Level Backup`:

- физическое копирование файлов кластера;
- быстро восстанавливается;
- подходит для больших БД;
- требует согласованного состояния файловой системы.

`Continuous Archiving`:

- основан на архивировании WAL;
- позволяет выполнять PITR;
- обеспечивает минимальную потерю данных;
- требует аккуратного хранения WAL и контроля за их объемом.

На практике наиболее надежной является комбинация полной физической копии и непрерывного архивирования WAL.

### 3. Как рассчитать объем пространства для хранения резервных копий, от чего зависит

Объем хранения зависит от:

- размера полной копии;
- интенсивности изменений;
- частоты создания копий;
- срока хранения;
- числа поколений резервных копий;
- коэффициента сжатия;
- накладных расходов WAL и служебных данных.

Базовая формула:

```text
V = Nfull * Vfull + Ndays * Vwal_day + Vreserve
```

### 4. Как рассчитать требуемую производительность канала для выполнения резервных копий по сети, от чего зависит

Необходимая пропускная способность канала оценивается формулой:

```text
Bandwidth >= V / T
```

где:

- `V` - объем передаваемых данных;
- `T` - допустимое окно резервного копирования.

На требуемую производительность влияют:

- размер базовой копии;
- объем WAL;
- допустимое время передачи;
- шифрование и сжатие;
- сетевые задержки;
- скорость дисковой подсистемы на обоих узлах.

### 5. Какие факторы оказывают влияние на скорость восстановления, почему

На скорость восстановления влияют:

- размер базовой копии;
- объем WAL, который необходимо применить;
- скорость чтения резервной копии;
- скорость записи на целевой диск;
- число табличных пространств;
- количество индексов и общая сложность структуры БД;
- необходимость восстановления на конкретный момент времени;
- производительность CPU и объем доступной памяти.

Чем больше WAL нужно проиграть после восстановления базовой копии, тем дольше длится процесс запуска кластера после аварии.

---

## Вывод

В ходе выполнения лабораторной работы была настроена схема резервного копирования PostgreSQL по модели «полная физическая копия + непрерывное архивирование WAL» с переносом данных на резервный узел. Были подготовлены сценарии для аварийного запуска базы данных на резервном сервере, полного восстановления основного узла после физического повреждения табличного пространства и восстановления логически поврежденных данных через PITR на резервном узле и последующий `pg_dump`/`pg_restore` на основном узле.

В результате была подтверждена работоспособность выбранной схемы резервного копирования и показано, что комбинация `pg_basebackup` и архивных WAL позволяет восстанавливать как весь кластер, так и отдельные объекты базы данных.
